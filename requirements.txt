# Minimal dependencies for Render deployment
# Only using pure Python packages with no compilation required

# Web framework
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0

# HTTP requests
requests==2.31.0

# Environment variables
python-dotenv==1.0.0

# File uploads
python-multipart==0.0.6

# Note: Removed PyPDF2 as it requires Rust compilation
# Will use alternative PDF processing approach
