# Core dependencies for LLM-powered query–retrieval system
PyMuPDF==1.23.8
python-docx==1.1.0
extract-msg==0.41
faiss-cpu==1.7.4
numpy==1.24.3
requests==2.31.0
python-dotenv==1.0.0
chromadb==0.4.22

# LangChain dependencies - using more stable versions
langchain==0.0.350
langchain-community==0.0.10
langchain-core==0.1.10
langchain-text-splitters==0.0.1

# Web framework dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# Additional dependencies for document processing
python-multipart==0.0.6

# Temporarily remove problematic packages for initial deployment
# sentence-transformers and langchain-unstructured can be added later
# unstructured package can be added later if needed
