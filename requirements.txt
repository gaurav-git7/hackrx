# Core dependencies for Render deployment
requests==2.28.2
python-dotenv==1.0.0
fastapi==0.95.2
uvicorn==0.22.0

# PDF processing (essential)
PyPDF2==3.0.1
pdfplumber==0.9.0

# OCR and image processing (optional - will fallback if not available)
pdf2image==1.16.3
pytesseract==0.3.10
Pillow==10.0.0

# Text processing (optional - will fallback if not available)
transformers==4.35.0
torch==2.1.0
torchvision==0.16.0

# Utilities
numpy==1.24.3
