# Absolute minimal dependencies for Render deployment
# Testing with only the most basic packages

# HTTP requests (most basic)
requests==2.28.2

# Environment variables
python-dotenv==1.0.0

# Web framework (minimal)
fastapi==0.95.2
uvicorn==0.22.0
